enum AuthStatus { unknown, authenticated, unauthenticated }

enum AppLifecycleState { foreground, background, inactive }

enum StorageKeys { userThemeMode, authToken, userId }

enum TripStatus { open, closed }
